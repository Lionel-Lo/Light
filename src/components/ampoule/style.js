import { StyleSheet} from 'react-native';

export default StyleSheet.create({
    ampouleLogo: {
      width: 230,
      height: 360,
      position: 'absolute',
      top: 100,
      transform: [{rotate: '180deg'}],
    }
  }); 